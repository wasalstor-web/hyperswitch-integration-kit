# نسخ إلى .env وعدّل القيم — الملف .env لا يُرفع إلى git

# ─── Postgres مباشر (Prisma) ─────────────────────────────────
# مثال مع docker-compose.dev.yml: postgresql://app:app@127.0.0.1:5433/onboarding
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?schema=public"

# تشغيل بدون Docker: Postgres مضمّن (PGlite) — يتجاهل الاتصال بـ DATABASE_URL للاستعلام عبر Prisma adapter
# USE_PGLITE=true
# PGLITE_DATA_DIR=.data/pglite

# ─── خادم API المحلي (server) ───────────────────────────────
PORT=8788
PUBLIC_APP_VERIFY_URL=http://localhost:5173
# إنتاج: أصول مسموحة مفصولة بفواصل (CORS). بدونها يُستخدم * في التطوير.
# CORS_ORIGIN=https://app.example.com,https://admin.example.com
# اختياري: حماية الـ API — يجب أن يطابق VITE_INTERNAL_API_KEY في web/.env.local
# INTERNAL_API_KEY=

# تطوير فقط
DANGEROUS_RETURN_TOKEN=true
DANGEROUS_RETURN_OTP=true
# DANGEROUS_RETURN_HS_JWT=true

# بوابة رسائل (اختياري)
# MESSAGE_GATEWAY_URL=
# MESSAGE_GATEWAY_KEY=

# Hyperswitch (خطوة 5 — اختياري للتجربة بدون بلوحة)
# HYPERSWITCH_BASE_URL=
# HYPERSWITCH_ADMIN_API_KEY=
# HYPERSWITCH_REGISTRATION_MODE=public_signup

# بوابة دفع (OAuth2 تجريبي — ليست MESSAGE_GATEWAY ولا Hyperswitch مباشرة)
# احصل على PAYMENT_GATEWAY_TOKEN_URL من وثائق المزود (مسار طلب التوكن).
# PAYMENT_GATEWAY_TOKEN_URL=https://provider.example/oauth/token
# PAYMENT_GATEWAY_CLIENT_ID=
# PAYMENT_GATEWAY_CLIENT_SECRET=
# PAYMENT_GATEWAY_MERCHANT_PROFILE=your_merchant_profile_from_provider
# PAYMENT_GATEWAY_SCOPE=
# PAYMENT_GATEWAY_AUTH_MODE=form
# للتجربة فقط — مسار GET /functions/v1/payment-gateway-probe (عطّله بعد التحقق)
# PAYMENT_GATEWAY_PROBE_ENABLED=true

# EdfaPay — معرّف العميل = client_key، السر = Merchant password للتجزئة (انظر docs/EDFAPAY_MUBASAT_AR.md)
# EDFAPAY_CLIENT_KEY=
# EDFAPAY_HASH_PASSWORD=
# EDFAPAY_MERCHANT_PROFILE=
# EDFAPAY_MERCHANT_CODE=
# EDFAPAY_BASE_URL=https://apidev.edfapay.com
# EDFAPAY_USE_SANDBOX=true
# POST /functions/v1/edfapay-probe — تجربة SALE
# EDFAPAY_PROBE_ENABLED=true

# ─── واجهة الويب (web/.env.local منفصل) ─────────────────────
# VITE_API_BASE_URL=http://localhost:8788
# VITE_INTERNAL_API_KEY=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
